CREATE TABLE characters (
  id INTEGER PRIMARY KEY,
  name TEXT,
  motto TEXT,
  species TEXT
);

CREATE TABLE books (
  id INTEGER PRIMARY KEY,
  title TEXT,
  year

);

CREATE TABLE series (
  id INTEGER PRIMARY KEY,
);

CREATE TABLE authors (
  id INTEGER PRIMARY KEY,
);

CREATE TABLE subgenres (
  id INTEGER PRIMARY KEY,
);
